<br />
<ul>
<% for user in @users %>
	<li><%= image_tag user.image.url(:small) %>  <%= link_to user.name, user %> 
	<% if signed_in? && current_user.has_role?('Administrator') %>
		[ <%= link_to 'delete', user, :confirm => 'Are you sure?', :method => :delete %>]
	<% end %></li>
<% end %>
</ul>